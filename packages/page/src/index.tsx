import * as React from 'react'
import ReactDOM from 'react-dom'
import RGenerate from './r-generator'

ReactDOM.render(
  /* <React.StrictMode> */
  <RGenerate />,
  /* </React.StrictMode>, */ document.getElementById('root')
)
